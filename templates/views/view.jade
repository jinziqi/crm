extends ../layouts/default

block content
    .container: .row: .col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
            p: a(href='/case') &larr; 返回
            hr
            if !data.post
                h2 Invalid Case.
            else
                h1= data.post.name


